## Overview
Creates a knowledge graph and a `conventions.md` file given (a) Python file(s).

Chose Python to narrow scope + easier to debug in case things went wrong, especially considering time-crunch (3-hours). 

## Future work that I'd add
1) Finish steps 10-13 (see Plan.md that was used for vibecoding this).
2) MUI MCP made building this pretty easy, though I'm not sure I like how it looks. Make it not so Androidy.
3) Add the ability to ingest a repo and creat a `conventions.md` that encapsulates the app.
4) Add a more useful feature using the knowledge graph build (e.g add doc-strings and create documentation)
5) More languages would be cool
